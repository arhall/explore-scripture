---
layout: base.njk
title: Bible Explorer
description: Chapter-by-chapter Bible summaries, videos, and commentary for every book of Scripture.
---

<div class="hero-section">
  <div class="hero-content">
    <h1>Bible Explorer</h1>
    <p class="hero-description">Discover the unified story of Scripture through chapter summaries, Bible Project videos, and expert commentary.</p>
    
    <div class="hero-stats">
      <div class="stat">
        <div class="stat-number">66</div>
        <div class="stat-label">Books</div>
      </div>
      <div class="stat">
        <div class="stat-number">1,189</div>
        <div class="stat-label">Chapters</div>
      </div>
      <div class="stat">
        <div class="stat-number">{{ books | selectattr("chapterSummaries") | list | length }}</div>
        <div class="stat-label">With Summaries</div>
      </div>
    </div>
  </div>
</div>

<div class="features-grid">
  <div class="feature-card">
    <div class="feature-icon">📚</div>
    <h3>Chapter Summaries</h3>
    <p>Concise, theologically accurate summaries for every chapter</p>
  </div>
  <div class="feature-card">
    <div class="feature-icon">🎥</div>
    <h3>Bible Project Videos</h3>
    <p>Animated overviews explaining each book's themes and structure</p>
  </div>
  <div class="feature-card">
    <div class="feature-icon">💬</div>
    <h3>Expert Commentary</h3>
    <p>Direct links to trusted biblical commentaries and resources</p>
  </div>
  <div class="feature-card">
    <div class="feature-icon">🔍</div>
    <h3>Smart Search</h3>
    <p>Find books, chapters, and topics instantly with our search</p>
  </div>
</div>

<section class="testament-section">
  <h2>Old Testament</h2>
  <p class="section-intro">God's covenant with Israel and the promise of redemption</p>
  
  {% for category in categories %}
    {% if category.testament == "Old Testament" %}
      <div class="category-group">
        <div class="category-header">
          <h3>
            <a href="/categories/{{ category.slug }}/">{{ category.name }}</a>
            <span class="book-count">{{ category.bookCount }} books</span>
          </h3>
          <p class="category-description">{{ category.description }}</p>
        </div>
        
        <div class="category-books">
          {% for book in books %}
            {% if book.category == category.name %}
            <a href="/books/{{ book.slug }}/" class="book-card">
              <div class="book-title">{{ book.name }}</div>
              {% if book.chapterSummaries and (book.chapterSummaries | length) > 0 %}
                <div class="book-progress">{{ book.chapterSummaries | length }} chapters</div>
              {% else %}
                <div class="book-progress pending">Coming soon</div>
              {% endif %}
            </a>
            {% endif %}
          {% endfor %}
        </div>
      </div>
    {% endif %}
  {% endfor %}
</section>

<section class="testament-section">
  <h2>New Testament</h2>
  <p class="section-intro">The life of Christ and the birth of the Church</p>
  
  {% for category in categories %}
    {% if category.testament == "New Testament" %}
      <div class="category-group">
        <div class="category-header">
          <h3>
            <a href="/categories/{{ category.slug }}/">{{ category.name }}</a>
            <span class="book-count">{{ category.bookCount }} books</span>
          </h3>
          <p class="category-description">{{ category.description }}</p>
        </div>
        
        <div class="category-books">
          {% for book in books %}
            {% if book.category == category.name %}
            <a href="/books/{{ book.slug }}/" class="book-card">
              <div class="book-title">{{ book.name }}</div>
              {% if book.chapterSummaries and (book.chapterSummaries | length) > 0 %}
                <div class="book-progress">{{ book.chapterSummaries | length }} chapters</div>
              {% else %}
                <div class="book-progress pending">Coming soon</div>
              {% endif %}
            </a>
            {% endif %}
          {% endfor %}
        </div>
      </div>
    {% endif %}
  {% endfor %}
</section>
