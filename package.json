{
  "name": "bible-static-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "build:analyze": "eleventy && node build-logger.js",
    "build:production": "eleventy && node build-logger.js",
    "logs:analyze": "node build-logger.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}